# content of: tox.ini , put in same dir as setup.py
[tox]
envlist = py36,py39

[testenv]
# install pytest in the virtualenv where commands will be executed
deps =
    pytest
    pytest-cov
    xmltodict
commands =
    pytest --cov src --cov-branch --cov-report term-missing --cov-fail-under 95
