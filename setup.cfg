[metadata]
name = enadownloader
version = 2.0.1

[options]
packages=find:
package_dir=
         =src

[options.packages.find]
where=src

[options.entry_points]
console_scripts =
    enadownloader = enadownloader:main