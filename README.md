# ENA Download
WIP

## Install
### Development
`pip install -e .`

### Production
`pip install -e .`

## Distribution
`python -m build`
This creates a `dist` folder containing a `.whl` that can be installed using `pip install <xxx>.whl`
